<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.todo.model.dao.TodoDao">
  <select id="selectAllTodo" resultType="todo">
  	select * from todo_tbl order by 1
  </select>
  <insert id="insertTodo">
  	insert into todo_tbl values
  	(
  		todo_seq.nextval,
  		#{todoContent},
  		#{todoWriter},
  		0,
  		to_char(sysdate,'yyyy-mm-dd')
  	)
  </insert>
  
  <select id="selectOneTodo" resultType="todo">
  	select
  		todo_writer,
  		todo_content,
  		to_char(todo_date,'yyyy-mm-dd hh24:mi') todo_date
  	from todo_tbl where todo_content = #{todoContent}
  </select>
</mapper>
